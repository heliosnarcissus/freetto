const state = {
  token: 'vvvvvvvvvssscsss'
}

const getters = {
  isToken: (state) => {
    return state.token
  }
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
